@namespace Talk.Client.Slides

<Slide>
    <div class="reveal">
        <p class="r-lead">Every slide you'll see today <span class="hl">is a Blazor component.</span></p>
        <p class="r-body">This deck is a Blazor Web App — parameters, state, cascading values,
            templates, JS interop: the whole toolkit.</p>
        <p class="r-body">By the end, you'll understand every piece of it.</p>
        <p class="r-hint"><kbd>→</kbd> advances &middot; <kbd>t</kbd> themes &middot; <kbd>o</kbd> overview</p>
    </div>
</Slide>

<style>
    .reveal { display: flex; flex-direction: column; gap: 1.5rem; max-width: 46ch; }
    .r-lead { font-size: clamp(1.8rem, 4.5vw, 3rem); font-weight: 700; line-height: 1.15; margin: 0; }
    .r-lead .hl { color: var(--accent); }
    .r-body { margin: 0; opacity: 0.85; max-width: 40ch; }
    .r-hint { margin: 1rem 0 0; opacity: 0.55; font-size: 1rem; }
    .r-hint kbd { background: #20232b; border: 1px solid #333; border-radius: 4px; padding: .05em .45em; }
</style>
