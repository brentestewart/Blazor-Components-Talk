@namespace Talk.Client.Slides

@* Concept #9 — how components talk. A map that frames the whole segment: params flow
   down, EventCallback flows up, and shared state (a DI service) reaches anywhere — the
   three mechanisms the rest of Segment C dissects in the real deck. *@
<Slide Title="How components talk to each other">
    <div class="cm">
        <div class="cm-lane down">
            <div class="cm-arrow">↓ down</div>
            <div class="cm-what"><code>[Parameter]</code></div>
            <div class="cm-desc">Parent hands values <em>down</em> to a child.<br /><span class="cm-eg">Segment B — <code>&lt;Slide Title="…"&gt;</code></span></div>
        </div>
        <div class="cm-lane up">
            <div class="cm-arrow">↑ up</div>
            <div class="cm-what"><code>EventCallback</code></div>
            <div class="cm-desc">Child raises an event back <em>up</em> to its parent.<br /><span class="cm-eg">Segment B — <code>&lt;Stepper OnStep="…"&gt;</code></span></div>
        </div>
        <div class="cm-lane any">
            <div class="cm-arrow">↔ anywhere</div>
            <div class="cm-what"><code>DeckState</code></div>
            <div class="cm-desc">A shared service any component can read &amp; change — no parent link needed.<br /><span class="cm-eg">the rest of this segment ↓</span></div>
        </div>
    </div>
    <p class="cm-note">Down and up need a parent–child wire. When two far-apart components
        need the same state, you reach for the third path.</p>
</Slide>

@code {
}

<style>
    .cm { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1.25rem; margin-top: 0.5rem; }
    .cm-lane {
        display: flex; flex-direction: column; gap: 0.6rem;
        padding: 1.3rem 1.2rem; border-radius: 14px;
        border: 1px solid var(--deck-border);
        background: color-mix(in srgb, var(--deck-surface) 60%, transparent);
    }
    .cm-lane.any {
        border-color: color-mix(in srgb, var(--accent) 55%, var(--deck-border));
        background: color-mix(in srgb, var(--accent) 8%, transparent);
    }
    .cm-arrow {
        font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.1em;
        opacity: 0.7; font-weight: 700;
    }
    .cm-what { font-size: 1.35rem; }
    .cm-what code { color: var(--accent); font-weight: 600; }
    .cm-desc { font-size: 1rem; line-height: 1.5; opacity: 0.85; }
    .cm-eg { opacity: 0.7; font-size: 0.9rem; }
    .cm-eg code, .cm-desc code { color: var(--accent); }
    .cm-note { margin: 1.4rem 0 0; opacity: 0.75; max-width: 70ch; font-size: 1.1rem; }
</style>
