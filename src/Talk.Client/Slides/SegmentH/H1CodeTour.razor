@namespace Talk.Client.Slides

@* Segue into the live tour of the real project: switch to the editor, walk the structure, and
   point out the reusable pieces along the way — the BlazorDeck RCL and the MVVM starter in
   samples/mvvm. Deliberately no link here; the GitHub URL lives on the closing slide. *@
<SectionSlide Marker="Live" Title="Let's look at the code"
              Subtitle="This whole deck is a real project — let's walk it." />
